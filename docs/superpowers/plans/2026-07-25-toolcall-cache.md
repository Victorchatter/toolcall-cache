# toolcall-cache — implementation plan (pointer)

Implementation actually proceeded from the root-level
[`PLAN.md`](../../../PLAN.md), written and executed before this
`docs/superpowers/plans/` convention was retrofitted onto the project. That
file holds the real phase breakdown (bootstrap → core primitives → stdio
transport → HTTP transport → CLI → selfcheck → diagrams/README → final
verification) and is the source of truth for how the build happened.

This file exists only to satisfy the repo's directory convention
(`docs/superpowers/plans/YYYY-MM-DD-<name>.md`) without duplicating that
content. See [`docs/superpowers/specs/2026-07-25-toolcall-cache-design.md`](../specs/2026-07-25-toolcall-cache-design.md)
for the design spec.
